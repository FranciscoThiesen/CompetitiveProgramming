#include <bits/stdc++.h>

using namespace std;

#define rep(i, a, b) for(int (i) = (a); (i) < (b); ++(i))
#define all(v) (v).begin(), (v).end()
#define pb push_back

using vi = vector<int>;
using pii = pair<int, int>;

struct centroid {
     
};

int main()
{

}
